import { Stack, Title } from '@mantine/core';

export function Requirements() {
  return (
    <Stack>
      <Title order={1}>Requirements</Title>
      {/* TODO: Add requirements content */}
    </Stack>
  );
}

export default Requirements; 